# Contributing

感谢你帮助改进“坐哪儿”。

## 开始之前

1. 使用 Node.js `>=22.13.0`。
2. 运行 `npm install`。
3. 从独立分支提交改动。
4. 提交前运行 `npm run lint` 和 `npm test`。

## 数据贡献

影院规格和座位排列必须附带可追溯来源、采集时间以及允许使用或再分发的依据。不要提交实时订单、售票状态、账号信息或其他个人数据。

几何估算与实测数据必须明确区分。若数据并非影院官方测绘，请在字段或说明中标记为估算。

## 代码贡献

- 保持桌面端和移动端可用。
- 为用户可见行为补充或更新测试。
- 不提交密钥、令牌、登录态或本机配置。
- 不加入未经授权的品牌素材、影片或抓取数据。
