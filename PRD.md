# 「坐哪儿」影院视野模拟器 PRD

## 1. 产品目标

帮助不了解 IMAX 和大型影厅差异的观众，在购票前直观理解某个真实影厅、某个具体座位的屏幕占比、视线角度与整体沉浸感。

首个版本验证一件事：用户是否能通过 3D 视角和选座交互，更有把握地选择影厅与座位。

## 2. 目标用户

- 第一次选择 IMAX、杜比影院等高规格影厅的普通观众
- 对前排、后排、侧边座位体验缺少空间概念的购票用户
- 希望在多人观影前快速比较座位的电影爱好者

## 3. 核心场景

用户打开页面，选择一个真实电影院和影厅，在座位图中选择具体座位。页面立即切换到该座位的人眼视角，用户可以拖动视角观察影厅，也可以在放映前与放映中两种状态间切换，并播放一段短片确认屏幕带来的沉浸感。

## 4. 功能范围

### P0

- 展示一个基于真实 IMAX 规格整理的样例影厅
- 影院与影厅选择
- 10 排座位图，支持选择任意可用座位
- 3D 影厅、银幕、阶梯、墙体、座椅和引导灯
- 一键进入所选座位的人眼视角
- 默认从推荐座位的人眼视角进入，可开启自由视角观察全厅
- 拖动观察，按钮微调视角
- 放映前 / 放映中切换
- 银幕播放、暂停一段本地短视频
- 根据座位实时计算屏幕水平视角、垂直仰角和推荐提示
- 桌面端与手机端响应式布局
- 低性能设备自动降低像素比、阴影与细节

### P1

- 接入更多影院与影厅实测数据
- 影院搜索、城市筛选、定位附近影院
- 对比两个座位
- 分享具体影院、影厅、座位的链接
- 维护端录入影厅数据与座位布局

## 5. 非目标

- 不在首版提供购票、支付或账号系统
- 不声称样例数据是影院官方测绘数据
- 不模拟音响声场、爆米花气味或真实观众遮挡
- 不追求电影级离线渲染，优先保证手机浏览器流畅

## 6. 关键流程

1. 打开页面，加载影厅样例数据并进入推荐座位的人眼视角
2. 选择影院与影厅
3. 在座位图中点选座位
4. 查看座位指标与推荐说明
5. 拖动或按按钮调整观察方向
6. 切换到放映中，播放短片
7. 开启自由视角观察全厅，重新选座后回到座位视角

## 7. 数据对象

### Cinema

- `id`
- `name`
- `city`
- `address`

### Auditorium

- `id`
- `cinemaId`
- `name`
- `format`
- `screenWidth`
- `screenHeight`
- `screenBottom`
- `roomDepth`
- `rowCount`
- `seatSpacing`
- `rowSpacing`
- `aisles`
- `dataSource`

### Seat

- `id`
- `row`
- `number`
- `x`
- `y`
- `z`
- `status`
- `type`

## 8. 页面与状态

单页产品：

- 顶部：品牌、影院/影厅选择、放映状态
- 主区：3D 影厅画布
- 叠加控制：自由视角开关、视角微调、视频播放
- 底部/侧栏：座位图、当前座位指标、数据说明

需要覆盖：

- 3D 加载状态：与画布同尺寸的暗色骨架屏
- 视频加载失败：银幕回退为静态电影画面
- WebGL 不可用：显示清晰的设备兼容提示
- 暂无座位：保留影厅信息并说明布局数据尚未录入
- 手机端：画布优先，控制条和座位图改为纵向区域

## 9. 响应式与交互

- 最小支持宽度 320px
- 手机端画布高度约 54dvh，避免地址栏导致跳动
- 所有按钮触控区域不小于 44px
- 拖动旋转仅改变视线方向，不改变真实座位位置
- 键盘可聚焦所有按钮与座位
- 尊重 `prefers-reduced-motion`

## 10. 视觉方向

- 以真实影院黑场为主，不做通用科技渐变
- 暗灰吸音墙、深红绒面座椅、暖白踏步灯、冷白银幕
- 未放映银幕使用宽视角增益幕光学模型，呈现浅银白基底、细微珠光颗粒、侧向亮度衰减和轻微曲率
- 微孔按真实开孔率影响整体反射，正常座位距离不夸张显示单个孔洞
- 控件使用半透明黑色面板和细边框，避免遮挡 3D 场景
- 单一强调色为影院红
- 圆角规则：面板 14px、控件 10px、状态标签为胶囊

## 11. 技术约束

- React + TypeScript
- Three.js，通过 React Three Fiber 组织场景
- 不依赖后端，样例数据随静态资源发布
- 短视频作为本地静态资源，支持静音自动播放失败后的手动播放
- 3D 首屏按需加载
- 移动端像素比上限 1.5，桌面端上限 2
- 使用实例化或复用几何体降低 draw call

## 12. 验收标准

- 页面加载后直接呈现推荐座位的人眼视角
- 选中座位后，3D 摄像机位置与指标同步更新
- 放映状态切换会改变全厅灯光、银幕亮度与视频状态
- 银幕视频可播放、暂停并循环
- 更换中央与侧边座位时，银幕亮度分布依据增益和半增益角变化；仅旋转视线不会产生贴图滑动
- 320px 宽屏幕上无水平页面溢出，主要控件可触达
- 桌面和主流手机浏览器能稳定维持可交互帧率
- 生产构建通过，页面无运行时错误

## 13. 运行环境与交付物

- 现代桌面与手机浏览器，要求 WebGL 2 或可用的 WebGL
- 可运行源码
- 一键启动配置
- README 与数据扩展说明
- 可部署的站点版本

## 14. 数据说明

首版影厅为产品验证样例。尺寸参考典型大型 IMAX GT 厅比例整理，并明确标记为非影院官方测绘数据。幕面模型参考影院幕布厂商公开的 1.4 增益、85° 半增益角、0.9mm 数字微孔和 4.16% 开孔率参数，不代表当前影院实际安装型号。要达到购票决策级准确度，后续需要影院方提供 CAD、座椅表、银幕尺寸、首排距离、排距与高差，以及幕面型号、增益和曲率数据。
